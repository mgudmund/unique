name: Inspect Contexts

on: [push]

jobs:
  build:
    name: Inspect contexts
    runs-on: ubuntu-latest
    steps:
      - name: step1
        run: |
          ls -la /data
          ls -la /home/runner  
      
